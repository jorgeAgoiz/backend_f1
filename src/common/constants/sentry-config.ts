export enum logLevels {
  fatal = 'fatal',
  critical = 'critical',
  error = 'error',
  warning = 'warning',
  log = 'log',
  info = 'info',
  debug = 'debug',
}

export enum environmentTypes {
  development = 'development',
  test = 'test',
  production = 'production',
}
